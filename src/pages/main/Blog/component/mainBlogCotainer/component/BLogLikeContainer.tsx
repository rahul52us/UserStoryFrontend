import { Box } from "@chakra-ui/react";

const BLogLikeContainer = () => {
  return (
    <Box position="sticky" left={0} top={20} zIndex={100}>
      BLogLikeContainer
    </Box>
  );
};

export default BLogLikeContainer;